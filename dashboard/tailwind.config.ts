import type { Config } from 'tailwindcss'

// Tailwind v4: tokens definidos via @theme em globals.css
const config: Config = {
  content: ['./index.html', './src/**/*.{ts,tsx}'],
}

export default config
